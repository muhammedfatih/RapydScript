import stdlib

styles = {
    'background-color': '#ffe',
    'border-left':      '5px solid #ccc',
    'width':            50,
}

result=dict.pop(styles, "width")
console.log(styles)
console.log(result)
